# Path to the Play Console service account JSON key. CI writes this from a
# secret before running fastlane; for local use, set SUPPLY_JSON_KEY_FILE to
# a local copy of that key (never commit it). See docs/ANDROID_RELEASE_AUTOMATION.md.
json_key_file(ENV["SUPPLY_JSON_KEY_FILE"])
package_name("org.getspot")
